fn main() -> void {
    // variables shouldn't begin with digits and shouldn't
    // contain special characters
    // (":" is used for type adnotation and ")" is used for grouping)
    let 321a:)x: int;

    // char literal too long
    if 321:)x > 0 {print('greater than zero'); }
    else { print("smaller than zero"); }
}




